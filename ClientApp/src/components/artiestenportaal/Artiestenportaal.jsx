import React, { useEffect, Component }  from 'react';
import ReactDOM from 'react-dom';
import { useState } from 'react';
import { Layout } from './Layout';

export class Artiestenportaal extends Component {
    render() {
        return (
            <Layout />
        );
    }
}